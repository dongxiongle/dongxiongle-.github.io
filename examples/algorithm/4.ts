/**
 * 给你两个单词word1和word2，计算将word1转为word2所使用的最少操作数。你可以对一个单词进行如下三种操作：插入一个字符、删除一个字符、替换一个字符
 * @param a 
 * @param b 
 */
const minDistance = (a: string, b:string) => {
  let distance = 0;
  return distance;
}